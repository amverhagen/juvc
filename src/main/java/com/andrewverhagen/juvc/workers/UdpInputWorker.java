package com.andrewverhagen.juvc.workers;

import java.util.Observable;
import java.util.Observer;

public class UdpInputWorker extends Thread implements Observer {

    @Override
    public void update(Observable o, Object arg) {

    }
}
